package io.herow.sdk.detection.notification.model

enum class DynamicKeys(val value: String) {
    RADIUS("zone.radius"),
    NAME("zone.name"),
    ADDRESS("zone.address"),
    CUSTOM_ID("user.customId")
}